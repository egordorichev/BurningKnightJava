player = extend(creature)

defineCreature(player, "player", {
	hpMax = 8
})

defineState(player, "idle", {
	onEnter = function(state, self)

	end,

	onExit = function(state, self)

	end,

	update = function(state, self, dt)

	end
})